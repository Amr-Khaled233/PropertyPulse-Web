# Web environment — copy to apps/web/.env (only VITE_* vars are exposed to the client)
VITE_API_URL=http://localhost:4000/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
