// Web model layer: re-exports shared-types + view-specific presentation models.

export * from '@propertypulse/shared-types';
export type { PropertySearchParams, AsyncState, WatchlistEntry } from '../types';
